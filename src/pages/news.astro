---
import { loadExchanges, footerStats } from '../lib/data.js';
import '../styles/global.css';

const exchanges = loadExchanges();
const stats = footerStats(exchanges);

// One row per (brand, mention) pair -- a brand can have several matches.
// Pure chronological (newest first), same as news.xml's RSS feed --
// previously sorted regulatory-tagged mentions first regardless of date,
// which meant a week-old regulatory item could sit above a same-day
// general one, reading as out of order despite each group individually
// being date-sorted. The Regulatory/General tag is still shown per row;
// it just no longer overrides date order.
const rows = exchanges
  .flatMap((ex) => (ex.news_mentions ?? []).map((n) => ({ ex, ...n })))
  .sort((a, b) => (b.published || '').localeCompare(a.published || ''));

const SITEKEY = import.meta.env.PUBLIC_TURNSTILE_SITEKEY || '0x4AAAAAAD84t4OWkoouLP3Y';
const PAGE_TITLE = `Crypto Exchange News and Regulation Updates | whichcryptoexchange.com`;
const PAGE_DESC = `Headlines that name-match a tracked crypto exchange, from The Guardian, BBC, Cointelegraph, The Block, and the SEC and DOJ's own press releases -- newest first, tagged Regulatory when the source or the headline itself is regulatory in nature.`;

const PAGE_URL = 'https://whichcryptoexchange.com/news/';
const SHARE_TEXT = 'Crypto exchange news and regulation updates, tagged by source (SEC/DOJ direct vs. general coverage) — not just headlines.';
const SHARE_LINKS = [
  { name: 'X', href: `https://twitter.com/intent/tweet?text=${encodeURIComponent(SHARE_TEXT)}&url=${encodeURIComponent(PAGE_URL)}` },
  { name: 'LinkedIn', href: `https://www.linkedin.com/sharing/share-offsite/?url=${encodeURIComponent(PAGE_URL)}` },
  { name: 'Reddit', href: `https://www.reddit.com/submit?url=${encodeURIComponent(PAGE_URL)}&title=${encodeURIComponent(SHARE_TEXT)}` },
];
---
<html lang="en">
<head>
  <meta charset="utf-8" />
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{PAGE_TITLE}</title>
  <meta name="description" content={PAGE_DESC} />
  <link rel="canonical" href={new URL(Astro.url.pathname, Astro.site)} />
  <link rel="alternate" type="application/rss+xml" title="whichcryptoexchange.com — Crypto Exchange News and Regulation Updates" href="/news.xml" />
  <meta property="og:type" content="website" />
  <meta property="og:url" content={new URL(Astro.url.pathname, Astro.site)} />
  <meta property="og:site_name" content="whichcryptoexchange.com" />
  <meta property="og:title" content={PAGE_TITLE} />
  <meta property="og:description" content={PAGE_DESC} />
  <meta property="og:image" content="https://whichcryptoexchange.com/og-image.jpg" />
  <meta property="og:image:width" content="1200" />
  <meta property="og:image:height" content="630" />
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content={PAGE_TITLE} />
  <meta name="twitter:description" content={PAGE_DESC} />
  <meta name="twitter:image" content="https://whichcryptoexchange.com/og-image.jpg" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link href="https://fonts.googleapis.com/css2?family=Archivo:wght@600;700;800&family=IBM+Plex+Mono:wght@400;600&display=swap" rel="stylesheet" />
  <script src="https://analytics.ahrefs.com/analytics.js" data-key="QTM3lN9yt0oU0rxrj5zadA" async></script>
  <!-- TrustBox script -->
  <script type="text/javascript" src="//widget.trustpilot.com/bootstrap/v5/tp.widget.bootstrap.min.js" async></script>
  <!-- End TrustBox script -->
</head>
<body>
  <header class="site">
    <div class="wrap">
      <a class="logo" href="/">which<em>crypto</em>exchange</a>
      <div class="header-right">
        <div class="site-search">
          <input type="search" id="site-search" placeholder="Search exchanges or countries…" autocomplete="off" aria-label="Search exchanges" />
          <div class="site-search-results" id="site-search-results" hidden></div>
        </div>
        <span class="sourced">source: official regulator registers</span>
      </div>
    </div>
    <nav class="site-nav"><div class="wrap">
      <a href="/exchanges/">Exchanges</a>
      <a href="/regulators/">Regulators</a>
      <a href="/providers/">Providers</a>
      <a href="/faq/">FAQ</a>
      <a href="/stats/">Stats</a>
      <a href="/reviews/">Reviews</a>
      <a href="/news/">News</a>
    </div></nav>
  </header>

  <main class="wrap">
    <h1>Crypto exchange news and regulation updates</h1>
    <p class="lede">Headlines naming a tracked exchange, newest first, checked daily — from The
    Guardian, BBC, Cointelegraph, and The Block, plus the SEC's and DOJ's own press-release feeds
    directly.</p>
    <p class="sourced">A mention is tagged <strong>Regulatory</strong> when it comes straight from
    a regulator's own newsroom (SEC, DOJ), or when the headline itself uses recognisably
    regulatory language (sues, charged, settlement, fined, and similar) — everything else is
    tagged <strong>General</strong>. Neither tag is a claim that a match is a formal action against
    the brand named, or that a General-tagged story is unrelated to its regulatory status — it's a
    keyword/source heuristic, not our own analysis. Read the original story before drawing a
    conclusion from it. An exchange with nothing here just hasn't been named in these sources'
    headlines recently — not a sign of anything either way.</p>

    <div class="page-tools">
      <a class="rss-link" href="/news.xml">RSS feed ↗</a>
      <span class="page-tools-share">
        Share:
        {SHARE_LINKS.map((s) => <a href={s.href} rel="noopener" target="_blank">{s.name}</a>)}
      </span>
    </div>

    {SITEKEY && (
      <div class="digest-cta">
        <h3>Get the weekly regulatory roundup</h3>
        <p>Every status change across every tracked exchange, once a week, straight to your inbox —
        the same underlying register this page draws from. Free, no spam — one email a week,
        unsubscribe any time.</p>
        <form id="digest-form">
          <div class="digest-cta-row">
            <input type="email" name="email" placeholder="you@example.com" required maxlength="200" aria-label="Email address" />
            <button type="submit">Subscribe</button>
          </div>
          <label class="consent-label">
            <input type="checkbox" name="consent" required />
            I want the weekly regulatory roundup email. See <a href="/about/">how this works</a> —
            unsubscribe any time, no login needed.
          </label>
          <div class="cf-turnstile" data-sitekey={SITEKEY}></div>
          <p class="form-msg sourced" role="status"></p>
        </form>
      </div>
    )}

    <div class="table-scroll">
    <table class="registry">
      <thead><tr>
        <th>Headline</th><th>Exchange</th><th>Category</th><th>Source</th><th>Published</th>
      </tr></thead>
      <tbody>
        {rows.map(({ ex, source, category, title, url, published }) => (
          <tr>
            <td><a href={url} rel="nofollow noopener" target="_blank">{title} ↗</a></td>
            <td class="brand"><a href={`/exchange/${ex.id}/`}>{ex.brand}</a></td>
            <td><span class={`news-category ${category === 'regulatory' ? 'regulatory' : 'general'}`}>
              {category === 'regulatory' ? 'Regulatory' : 'General'}
            </span></td>
            <td>{source}</td>
            <td class="datum">{published ?? '—'}</td>
          </tr>
        ))}
      </tbody>
    </table>
    </div>
    {rows.length === 0 && <p class="sourced">No matches yet — checked daily, nothing has hit so far.</p>}
  </main>

  <footer class="site"><div class="wrap">
    <div class="footer-top">
      <p><a href="/about/">About</a> · <a href="/contact/">Contact</a> · <a href="/changelog/">Changelog</a> · <a href="/exchanges/">Exchanges</a> · <a href="/regulators/">Regulators</a> · <a href="/stats/">Stats</a> · <a href="/reviews/">Reviews</a> · <a href="/news/">News</a> · <a href="/providers/">Providers</a> · <a href="/faq/">FAQ</a> · <a href="https://x.com/whichcex">X</a></p>
      <!-- TrustBox widget - Review Collector -->
      <div class="trustpilot-widget" data-locale="en-US" data-template-id="56278e9abfbbba0bdcd568bc" data-businessunit-id="6a79a88069053747f3a86e5d" data-style-height="52px" data-style-width="100%" data-token="4446dd34-abb7-486d-9d18-4cb40eab0921">
        <a href="https://www.trustpilot.com/review/whichcryptoexchange.com" target="_blank" rel="noopener">Trustpilot</a>
      </div>
      <!-- End TrustBox widget -->
    </div>
    <p class="footer-meta">{stats.brandCount} brands tracked across {stats.jurisdictionCount} jurisdictions · Data last synced {stats.lastSynced} · <a href="/changelog.xml">RSS</a></p>
    <p>Independent register. Not affiliated with any regulator. Not financial advice.</p>
  </div></footer>

  {SITEKEY && <script src="https://challenges.cloudflare.com/turnstile/v0/api.js" async defer></script>}
  <script src="/search.js" defer></script>
</body>
</html>
